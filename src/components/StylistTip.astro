---
interface Props {
  title?: string;
  text: string;
}

const { title = "Stylist's Tip", text } = Astro.props;
---

<div class="tip-wrap">
  <div class="tip-box">
    <p class="tip-label">{title}</p>
    <p class="tip-text">{text}</p>
  </div>
</div>
